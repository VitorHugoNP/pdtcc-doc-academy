@model IEnumerable<pdtcc_doc_academy.Models.Comunicados>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.dataComunicado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.fkDoc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.dataComunicado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.fkDoc)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idComunicados">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idComunicados">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idComunicados">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

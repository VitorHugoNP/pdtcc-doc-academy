@model IEnumerable<pdtcc_doc_academy.Models.RequerimentoEquipamento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.dataReq)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.dataReq)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdReq">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdReq">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdReq">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

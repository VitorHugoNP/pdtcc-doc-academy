@model IEnumerable<pdtcc_doc_academy.Models.Autorizacao>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.data_aut)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.fk_prot)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.data_aut)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.fk_prot)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idAutorizacao">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idAutorizacao">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idAutorizacao">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
